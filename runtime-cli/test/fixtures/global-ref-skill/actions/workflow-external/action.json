{
  "action_id": "workflow.external",
  "version": "1.0.0",
  "kind": "composite",
  "title": "Workflow External",
  "description": "Use a fully-qualified global action reference",
  "input_schema": {
    "type": "object",
    "properties": {
      "value": {
        "type": "number"
      }
    },
    "required": ["value"],
    "additionalProperties": false
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "value": {
        "type": "number"
      }
    },
    "required": ["value"],
    "additionalProperties": false
  },
  "visibility": "public",
  "side_effect": "none",
  "idempotent": true,
  "steps": [
    {
      "id": "addOne",
      "action": "cli://math/add-one",
      "with": {
        "value": "$input.value"
      }
    }
  ],
  "returns": {
    "value": "$steps.addOne.output.value"
  }
}
